package CS4125.Model.TrafficControl;

/**
 * Interface which describes the endpoint on a graph.
 */
public interface IEndpoint extends ITCM{
	// Future releases will add implementation to Endpoints, right now just want to specify if a node is an endpoint in a graph
	//	public abstract int getCapacity();
	//	public abstract void setCapacity(int capacity);
}